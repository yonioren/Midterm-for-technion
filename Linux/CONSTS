IMAGE="projectplanner"
TAG="0.0.1"
FULL_IMAGE_NAME="${IMAGE}:${TAG}"
SOURCE_IMAGE="${IMAGE}"
REPO_NAME="${IMAGE}" ### serves as IMAGE NAME in AWS #####
DEMO_CONTAINER_NAME="demo"

LINUX_DIR="$(dirname $(readlink -f $0))"
AWS_DIR="${LINUX_DIR}/../AWS"
PYTHON_DIR="${LINUX_DIR}/../Python"
AWS_CF_TEMPLATE="${AWS_DIR}/Cloudformation/template.yaml"
AWS_CF_DEST="${AWS_DIR}/projectplanner.yaml"
AWS_BEANSTALK_JSON="${AWS_DIR}/Beanstalk/Dockerrun.aws.json"

